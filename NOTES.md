# NOTES — voice-bridge

## 2026-06-13
Created project. FastAPI server scaffolded with rolling conversation history, Tailscale-only binding, and Sonnet 4.6 backend. Next: add ANTHROPIC_API_KEY to .env, run start.sh, wire up iOS Shortcut (recipe in Telegram conversation), and optionally set up launchd to auto-start on boot.

## 2026-06-13 (later — corrections + git)
The "next steps" above are superseded:
- **Backend is the `claude` CLI**, not the Anthropic SDK — the `ANTHROPIC_API_KEY` in `.env` is **vestigial/unused**.
- Runs as a **tmux service** via `~/bin/start-voice-bridge.sh` (started over SSH), **NOT launchd** — a LaunchAgent hit the no-FDA wall (`PermissionError` reading `.venv` under `~/Documents`) and crash-looped; the plist is disabled (`.disabled`).
- Made **project-aware + read-only**: `--append-system-prompt` + `--allowedTools Read,Glob,Grep` — grounds answers in real project files (e.g. reads NOTES.md) but can't write/edit or run shell commands. Refuses "do X" requests by voice.
- **Committed to git** this session (first commit). Endpoint is currently **unauthenticated** (Tailscale-only) — broadening tools / adding request auth is an open decision (owner sleeping on it); risk analysis in `mac-mini-ops` NOTES.

## 2026-06-14
iOS Shortcut built and working end-to-end. Apple Watch availability TBD (see Telegram conversation).

## 2026-06-14 (session 2)
- Done: Added Bearer token API key auth on /ask and /history (key in .env, read via _read_env_key to avoid polluting subprocess env — load_dotenv() was leaking vestigial ANTHROPIC_API_KEY and breaking Claude auth). Added Bash + Write + Edit to --allowedTools so voice agent can run permitted commands and author files. Added Linear access: personal API key (full access) stored in .env, passed as LINEAR_API_KEY to subprocess env; system prompt teaches Claude to query api.linear.app/graphql via curl. Removed broken mcp__claude_ai_Linear approach (cloud connector, not available in -p mode). All changes committed and pushed.
- Decided: _read_env_key() reads individual keys from .env without os.environ pollution — safer than load_dotenv() for a process that shells out to claude. Deploy/VPS operations prohibited unless owner says "deploy" explicitly. Linear write allowed (owner chose full-access key).
- Next: Test Linear queries and blog article writing via voice once watchOS update is complete and Watch shortcut is working.

## 2026-06-14 (session 3 — Watch shortcut sync diagnosis)
- Done: Long diagnostic on why the voice-bridge iOS Shortcut won't appear on the Apple Watch. Ruled out, in order: "Show on Apple Watch" toggle (on), unsupported actions (3× trivial dummy shortcuts also fail to appear), iCloud Drive (on), Watch storage (plenty), Apple ID mismatch (same account on iPhone/Watch/iCloud.com/Macs), and Watch-side corruption (full unpair + re-pair *with restore* didn't help). Key finding: **iCloud Notes sync perfectly across all 4 devices** → account/CloudKit is healthy → the problem is **isolated to the Shortcuts CloudKit sync zone**, which is wedged in a "deleted" tombstone state ("iCloud Shortcuts Deleted — Continue Syncing?" dialog recurs on the Mac). No device can push shortcuts to iCloud — edits, a duplicate, and a fresh **Mac-origin** shortcut all fail to propagate to iPhone/Watch. Not a beta OS; Developer Mode and the Tailscale VPN profile both ruled out as causes. Likely trigger: iCloud Drive Shortcuts data got deleted at some point (toggling iCloud Drive off wipes it).
- Decided: Root cause = wedged/corrupted **iCloud Shortcuts CloudKit zone**, Shortcuts-specific. The Watch can ONLY receive shortcuts via iCloud sync, so it stays blocked until the zone heals — re-pairing/toggling cannot fix it. The iPhone shortcut itself works fine; only the Watch trigger is on hold. Voice shortcut backed up as a shared iCloud link before touching sync.
- Next: (1) Click **Continue Syncing on the iPhone** and leave overnight on Wi-Fi — the "recently deleted" tombstone often self-clears within ~a day. (2) If still broken: **iCloud sign-out/in on the iPhone** to rebuild CloudKit zones — currently blocked by a "Sign out unavailable due to restrictions" message; re-verify Screen Time → Content & Privacy Restrictions → Account Changes = Allow. (3) Else **Apple Support** to reset the iCloud Shortcuts zone server-side, and file Feedback Assistant.

## 2026-06-14 (session 4 — long-task background jobs + spoken completion)
- Done: Fixed the timeout crash when asking by voice to write a tokn-watch article — the 120s `subprocess.run` cap raised an uncaught `TimeoutExpired` → 500. Reworked `/ask` to run the claude CLI via **async subprocess** with a 25s `SOFT_DEADLINE`: normal Q&A still returns synchronously and is spoken; longer tasks **detach** (15m `HARD_CAP`), return a spoken "Started, I'll let you know" ack, and on completion queue the reply + ring the phone. Loosened the system prompt so the agent stops refusing long work. Built the **spoken-completion** path: server rings a **static Pushcut notification** (doorbell) and queues the text; a `Speak Voice Result` Shortcut fetches it from the new auth'd **`/result/next`** endpoint and speaks it. Tested end-to-end — phone speaks. ✅ Updated CLAUDE.md (architecture, endpoints, env keys).
- Decided: Pushcut's **dynamic notification content is paid** (my earlier "free" claim was wrong) — so we **invert** it: static free doorbell + server-side `/result/next` fetch, zero subscription. Tradeoff accepted: on the free tier you **tap** the notification to hear the result (true auto-speak needs Pushcut's paid Automation Server, €44.99 lifetime — server unchanged if ever bought). New gitignored `results_queue.json` persists pending results across restarts.
- Next: Try a real "prepare the next tokn-watch article" run by voice now that the flow works (article #12 due 2026-06-15). Consider tuning `SOFT_DEADLINE` if borderline tasks feel awkward. Watch shortcut still blocked on the wedged iCloud Shortcuts zone (session 3).

## 2026-06-14 (session 4 cont. — Pushcut PRO assessment + Mac-mini auto-speak path)
- Done: Assessed paying for Pushcut PRO to get zero-tap auto-speak. Probed the mini for a Pushcut-free alternative: `say` is present (`/usr/bin/say`), default output is built-in **Mac mini Speakers**, and a 1080p display/GUI session is attached. Since voice-bridge already runs on the mini, on completion it could just call `say "<result>"` — no Pushcut, no PRO, no extra device, no tap.
- Decided: **Do NOT buy Pushcut PRO** for auto-speak. PRO unlocks dynamic content (minor) + the Automation Server, but reliable no-tap execution needs a *dedicated always-on iOS device* we don't have; on a daily-driver iPhone iOS suspends it. The free tap-to-hear setup is near the practical ceiling for a single phone. Better direction: use the always-on Mac mini (or the active Mac) to speak via `say`.
- Next: **Investigate Mac-mini `say` auto-speak.** Open questions: (1) does `say` actually emit audio from the **tmux-over-SSH service context** (audio routing from a non-foreground/SSH session is often blocked — test before building); (2) **earshot** — if the owner sits at the iMac and the mini is elsewhere, route speech to the *active* Mac instead (voice-bridge SSHes to the iMac to `say`, or a tiny listener on the iMac). Decide target device, then wire completion → `say` as an alternative/addition to the Pushcut doorbell.

## 2026-06-15 (Wispr Flow STT attempt — abandoned)
- Done: Tried swapping Apple **Dictate Text** for **Wispr Flow** dictation in the iOS Shortcut to improve transcription accuracy. Server unchanged — `/ask` just takes `{"text"}`, so this is a Shortcut-only change. It does NOT work cleanly: Wispr's iOS Shortcuts actions (`Quick Dictation to Clipboard`, `Create a Flow Note`) are **asynchronous** — they fire dictation and return immediately (metadata, not the transcript); the text lands on the **clipboard** only after you finish speaking + processing. Required a clipboard-polling loop (sentinel `__WAIT__` + Repeat → Get Clipboard → If/Wait). Then hit a persistent **"couldn't convert from Rich Text to URL"** Shortcuts error: Wispr's clipboard output is rich text, AND typing the URL into a Text action auto-links it to rich text (fix = type the URL **directly** into the Get-Contents-of-URL field, never via a Text action). `Create a Flow Note` exposes **no output variable**. Also: no Wispr **watchOS** app.
- Decided: **Abandoned Wispr; reverted to Apple Dictate Text** (synchronous — hands the transcript back inline, which is why the original just worked). Wispr Flow is not viable for chaining into an HTTP POST inside Shortcuts. The working shortcut was backed up as an **iCloud share link** before the experiment.
- Resolved: Restored the working Apple-dictation shortcut from the backup link — works end-to-end. Also this session: (a) reworded the long-task spoken ack in `server.py` to *"I'm checking. I will get back to you once done."* (killed the tmux server PID so the restart loop relaunched with the new code); (b) fixed *"Could not find the shortcut speak-voice-results"* — the Pushcut completion-speak shortcut had been **deleted** during today's iCloud Shortcuts cleanup; rebuilt it (`POST /result/next` + Bearer header → Speak, named to match the `voice-bridge-done` Pushcut action). Full long-task round-trip works again.
- Next: If higher STT accuracy is ever wanted, the realistic path is **server-side transcription** (record audio in the Shortcut → POST the audio → Whisper/whisper.cpp on the mini), NOT Wispr's Shortcut actions. Shortcut-building lessons logged: **type the URL directly into the Get-Contents-of-URL field** (never via a Text action — it auto-links to rich text and throws "couldn't convert Rich Text to URL"), keep the Bearer token **plain text**, and any Pushcut-launched shortcut's **name must match the Pushcut action exactly**.

## 2026-06-15 (session 2 — iCloud Shortcuts healed, Watch leg, TTS voice)
- Done: The wedged **iCloud Shortcuts CloudKit zone self-healed** (the "give it overnight, the tombstone clears" prediction from the session-3 Watch diagnosis came true) — shortcuts now sync **iMac ↔ MacBook ↔ iPhone** both ways. The **Apple Watch is the lone holdout**: even with **Show on Apple Watch** toggled on (which now has real iCloud data to pull, unlike when the zone was dead), the shortcut still doesn't appear on the Watch. Also: changed the **spoken-reply TTS voice** — picked a nicer voice in the shortcut's Speak Text action; it didn't carry to the Watch (watchOS has a smaller voice catalog and fell back to default), fixed by choosing a **standard voice present on both** devices.
- Decided: The Watch's **stale local Shortcuts store** — carried forward by the earlier *Restore from Backup* re-pair — is the likely remaining blocker. Speak Text uses ONE voice for both devices, so matching iPhone/Watch means a standard voice on both (premium/enhanced voices aren't on watchOS).
- Next: If the Watch still won't show shortcuts after a genuine charger+locked+Wi-Fi sync window (and a device restart), do the clean **unpair → re-pair → "Set Up as New Apple Watch"** (NOT Restore) — the one lever never tried; now that iCloud is healthy there's finally real data for it to sync.

## 2026-06-15 (session 3 — Watch sync resolved, delayed-answer playback, force-delay trigger)
- Done: **Watch sync is RESOLVED** — both shortcuts (ask + `Agent Reply` completion-speak) are now present on the Watch and short/synchronous answers come through fine. Built a **force-delay test trigger**: an `/ask` request starting with `test delay` / `delay test` / `delayed test` / `test the delayed answer` is forced down the background path (trigger stripped, remainder = the question; bare trigger → a default self-test prompt) so the delayed pipeline can be exercised on demand (commit 57a8f5d). Fixed *"details not found"* on playback: the `Agent Reply` shortcut POSTs to **`/results/next`** (plural) but the endpoint was `/result/next` (singular) → 404 → FastAPI's `{"detail":"Not Found"}` was read aloud; added a **plural alias** so the shortcut works as-is (commit 5650ada).
- Decided: **watchOS Siri can't suppress its completion confirmation** ("OK"/"Done") when a shortcut ends on `Speak`; the iOS space/empty `Stop and Output` tricks don't work on the Watch, and Siri **won't read a `Stop and Output` result aloud on the Watch** (only `Speak` does — Stop-and-Output just makes Siri say "done" and show a card). So the `Agent Reply` shortcut is just **Get Contents of `/results/next` → Speak `Contents of URL`** (no Stop and Output). Accept the cosmetic "OK" for hands-free Siri launch; the only way to avoid it is a **tap launch** (watch-face complication), not Siri.
- Next: Optional — fix the shortcut URL to singular `/result/next` and drop the server alias (cosmetic; both work now). Mac-mini `say` auto-speak investigation still open (session-4 cont.). Real voice test of writing tokn-watch article #12 still pending.

## 2026-06-15 (session 3 cont. — Siri trigger reference, no code change)
- Done: Confirmed final `Agent Reply` shortcut = **Get Contents `/results/next` → Speak `Contents of URL`** (Stop-and-Output removed; it broke Watch playback). No server/code changes this stretch — reference Q&A only.
- Reference (Siri triggering, so it isn't re-asked): the **Siri phrase IS the shortcut's name** — rename the shortcut to change it (rename syncs to Watch now that iCloud is healthy). The **"Hey Siri" wake word is NOT customizable** — Apple only offers "Siri" or "Hey Siri" (Settings → Apple Intelligence & Siri → "Listen for"); no custom wake word possible. On the **Watch**, skip the wake word via **Raise to Speak** or **hold the Digital Crown**. If `Agent Reply` is ever renamed, **re-point the Pushcut `voice-bridge-done` action** to the new name (Pushcut matches by exact name).
- Next: unchanged — Mac-mini `say` auto-speak investigation; real voice test of tokn-watch article #12.

## 2026-06-16
Wrote `HOW-IT-WORKS.md` — a shareable one-page doc explaining the full voice pipeline (user experience, step-by-step flow, components needed, agent permissions, endpoints, and how to run the server) pitched at a technical reader unfamiliar with this setup.

## 2026-06-16 (session 2)
- Done: Reviewed `HOW-IT-WORKS.md` and fixed an inaccuracy — the example request showed `https://` but the server runs plain **`http://`** (Tailscale provides the encryption; there is no TLS on the port). Committed + pushed.
- Resolved (TTS voice): **British female "Voice 3" now works on BOTH phone and Watch** — voice is consistent throughout. Corrects an earlier wrong theory of mine: the Watch *can* use the Siri-style "Voice 3" for Speak Text; it just needs that voice **downloaded on the Watch itself** — the "American male" was the Watch falling back *before* the voice was available there, not an inability to use it. Still true and worth keeping: the **Speak Text action's voice is independent of the Siri/VoiceOver voice settings** — set the voice IN the Speak Text action; changing Siri/VoiceOver alone does nothing to the agent.
- Next: A real voice test of writing tokn-watch article #12. **Dropped the Mac-mini `say` auto-speak idea** (session-4 cont.) — the current tap-to-hear Pushcut + Watch flow is sufficient; the owner has everything they need, so this thread is closed.

## 2026-06-16 (milestone — first fully hands-free article write + deploy)
- Done: Verified from the new records that **tokn-watch article 13 ("Claude's 5-Hour Session Limit, in Plain English") was written and deployed by voice from the Apple Watch on 2026-06-16** (~07:12 Berlin — owner recalled it as "yesterday"; it was this morning). It is **live**. The trail is captured in BOTH layers added this session: `voice-bridge/activity.jsonl` (every request/reply, timestamped) and `tokn-watch/NOTES.md` (a dated article-13 entry the agent wrote itself). The "no records" problem that started all this is solved.
- ⚠️ Honest caveat the log surfaced: the **first** "write and publish" run reported *"written, deployed, and live"* but the `scp` to the VPS **never ran** — the files existed only on the mini. The owner caught it by voice ("I can't see it on the website") and a **second** run (~07:22) actually pushed it. So hands-free end-to-end works, but the agent **overclaimed success on attempt 1** — a real reliability gap in the deploy path.
- Closes the long-standing "real voice test of writing a tokn-watch article" thread — done, with a deploy, from the Watch.
- Next (for the **tokn-watch agent**, not voice-bridge): it should **verify a deploy actually landed** (e.g. curl the live URL / check the appcast) before reporting success, so it can't falsely claim "deployed and live" again. voice-bridge itself is feature-complete and proven.

## 2026-06-16 (session — custom Siri prompt "Shoot")
- Done: Changed the ask shortcut's opening Siri prompt from the default **"What's the text?"** to **"Shoot"**. How: **Dictate Text has no editable prompt** (the default phrase is Siri's), so replaced it with an **Ask for Input** action (Type: Text, Prompt: `Shoot`) and pointed the POST body's `text` at that action's output (labeled **"Ask for Input"** in this iOS version = Provided Input). End-to-end works on the Watch.
- Gotcha logged: a **double prompt** ("Shoot" *then* "What's the text?") was caused by a **stray leftover variable in a field** (Wispr-era residue) that re-requested input — "What's the text?" only appears from a blank/extra input request. Removing the stray variable left a single "Shoot". Two expected behaviors, not bugs: the custom prompt is **slightly slower** to speak than Siri's instant built-in phrase (synthesis vs cached); and on the **phone**, Ask-for-Input via Siri shows a **typed field** rather than voice-capturing — the **Watch** (the device that matters) captures by voice fine.

## 2026-06-16 (session — voice agent granted email + Kindle authority)
- Done: Gave the voice agent authority to (1) **email the owner** and (2) **send docs to the owner's Kindle**, on request. `server.py` changes: added `Skill` to `--allowedTools`; added a **PERMITTED EMAIL & KINDLE** block to the system prompt — email goes ONLY through the `~/bin/agent-mail.py` policy wrapper (owner inboxes only: work/personal/kindle, 20/day, refuses third parties); Kindle via the **kindle skill** (pandoc→EPUB→send-to-Kindle) with a manual `pandoc + agent-mail.py --to kindle --attach` fallback; carved the agent-mail wrapper out of the no-outbound-requests prohibition. Restarted the tmux server. Verified first: pandoc present (`/opt/homebrew/bin/pandoc`) and mail config valid (`agent-mail.py --check`).
- Decided: **Bounded by design** — the wrapper enforces owner-only recipients, so granting this cannot exfiltrate to third parties (worst case the agent emails the owner). Kept the bans on editing the mail `.env` / adding recipients; refusal of any non-owner recipient is restated in the prompt.
- Next: Owner to voice-test "email me X" and "send X to my Kindle". If the kindle skill doesn't trigger in `-p` mode, the manual pandoc+agent-mail fallback in the prompt covers delivery.

## 2026-06-16 (session — email transport fixed; full record in mac-mini-ops)
- The voice agent's email broke on first use (Google blocks basic-auth SMTP for the agent Gmail: `534 WebLoginRequired`). Fixed by migrating the **shared** `agent-mail.py` wrapper from SMTP to the **Gmail API (OAuth)** — test send OK, the agent can email + Kindle again. This is **cross-project infra** (the mail wrapper serves every agent session + the Telegram bot), so the full record lives in **`mac-mini-ops` RUNBOOK §5f + NOTES (2026-06-16)**, not here. No voice-bridge code changed by this — the voice-bridge-specific part (granting the agent the email/Kindle *authority* in its system prompt) is the entry above.

## 2026-06-17
- Done: Decided to build a native iPhone + Apple Watch app (voice-bridge-ios) to replace the iOS Shortcuts front-end. Scaffolded the new project at `~/Documents/Projects/dev/voice-bridge-ios/` with ARCHITECTURE.md covering Swift/SwiftUI stack, WatchConnectivity flow, Keychain auth, and a four-phase build plan.
- Decided: Server stays untouched; app is a pure Swift front-end over Tailscale. Phase 1 = iPhone only; Phase 2 adds Watch via WatchConnectivity; Phase 3 targets native on-Watch speech recognition.
- Next: Review ARCHITECTURE.md and create the Xcode project.

## 2026-06-18 (long-task ack wording)
- Done: Changed the long-task spoken acknowledgement in `server.py` from "I'm checking. I will get back to you once done." to "I'm on it." — applies to both the force-background path and the slow-path fallback (lines 382 and 400). Server restart required to pick up the change.

## 2026-06-18 (Linear board consolidation)
- Done: Created a new "tokn-watch" project in the Spinbear Linear team and moved all 150 issues from the six P1–P6 phase projects into it. Team now has two clean projects: tokn-watch (150 issues, all phases) and GoSancho. P1–P6 projects are now empty shells and can be archived/deleted manually in Linear.

## 2026-06-18 (session 3 — Linear archiving + GoSancho board completion)
- Done: Archived oldest 20 completed BCK/SPI tickets (BCK-10–BCK-34 + SPI-158–SPI-165) → created SPI-236 as a reference ticket preserving all content. Then archived 30 completed tokn-watch tickets → created SPI-237 as a second reference ticket. Using the freed slots, created SPI-238–SPI-249 (12 issues) completing the GoSancho board Sessions 9 and 10 (settings polish, IAP unlock, open-source prep, and release checklist).
- Decided: Archive in batches with a single reference ticket per batch so content is never lost — the reference ticket pattern will be reused for future housekeeping rounds.
- Next: Archive P1–P6 empty shell projects in Linear (can now be done manually). Continue with GoSancho development.

## 2026-06-18 (session 4 — multi-consumer result delivery + APNs push, deployed live)
- Done: Two backward-compatible additions to `server.py`, driven by GoSancho needing reliable long-task delivery. (1) **Multi-consumer result contract** (commit `f021f01`): the result store is now an append-only log `{id, task_id, text, created_at}` (old list-of-strings migrated on read); new non-destructive `GET /v1/results?after=<id>` cursor read; `/ask` returns an `X-Agent-Task-Id` header (body still plain text); legacy `GET/POST /result(s)/next` preserved **byte-identically** via a `legacy_cursor`. (2) **APNs push** (`apns.py` + commit `de1f41a`): token-based ES256 JWT (pyjwt) over **HTTP/2** (httpx); new `POST /v1/devices` registry; on long-task completion, a **silent content-available wake** to registered devices (no result text in the payload — privacy); **client-aware routing** skips the Pushcut/Telegram doorbell when a device push is accepted, else falls back; dead tokens pruned; guarded import so missing deps/creds can't crash the server. Installed `h2` + `pyjwt[crypto]` in the venv; `APNS_*` added to `.env`, `.p8` at `~/.claude/apns/` (agent, 600); `devices.json` gitignored. Verified on an **isolated dev copy** (incl. a real APNs-sandbox round-trip) before deploying; restarted live as the `agent`-owned process; **verified live + on a physical device**. Wire spec: GoSancho `RESULT_DELIVERY_PROTOCOL.md`.
- Decided: One server with a grown-up, backward-compatible contract (not a forked/second server) — the first concrete **agent-bridge** step. Server stays subscription-Claude via the `claude` CLI.
- Next: result-ids enable future per-client routing; retire the legacy Pushcut→Shortcut path once APNs is trusted. Flip GoSancho's `aps-environment` to `production` for TestFlight.
